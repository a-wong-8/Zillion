import { useDispatch } from "react-redux";

export default function BuyShow() {
    const dispatch = useDispatch();

    return (
        <>

        </>
    )
}

// page that shows one selected house 